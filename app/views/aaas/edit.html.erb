<h>編集画面</h>

<%= form_with model:@aaa, url: update_aaa_path(@aaa.id), method: :patch do |f| %>

  <h4>タイトル</h4>
  <%= f.text_field :title %>

  <h4>内容</h4>
  <%= f.text_area :body %>

  <h4>日付</h4>
  <%= f.text_area :date %>

  <%= f.submit '保存する' %>

<% end %>